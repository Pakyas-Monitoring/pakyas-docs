---
phase: 03-updates-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/docs/cli/index.md
autonomous: true

must_haves:
  truths:
    - "User can find global flags documentation that applies to all commands"
    - "User can see --display-tz, --time, --no-color, --plain, --debug-http, --format flags documented"
    - "User can see which global flags have corresponding environment variables"
    - "User can find -y/--yes flag for check delete command"
    - "User can find --limit flag for check logs command"
  artifacts:
    - path: "src/content/docs/cli/index.md"
      provides: "Global flags section and updated check management examples"
      contains: "## Global Flags"
  key_links:
    - from: "src/content/docs/cli/index.md"
      to: "/docs/config/env-vars/"
      via: "See Also link"
      pattern: "env-vars"
---

<objective>
Update CLI index documentation with global flags section and command flag updates.

Purpose: Complete UPD-01, UPD-03, UPD-04 requirements - users need to know about global flags that work across all commands, plus specific flag updates for check delete and check logs commands.

Output: Updated cli/index.md with:
- New "Global Flags" section after existing "Environment Variables"
- Updated check delete example with -y/--yes flag
- Updated check logs example with --limit flag documentation
</objective>

<execution_context>
@/Users/light/.claude/get-shit-done/workflows/execute-plan.md
@/Users/light/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-updates-navigation/03-RESEARCH.md

Source reference:
@/Users/light/projects/pakyas/pakyas-docs/src/content/docs/cli/index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Global Flags section to CLI index</name>
  <files>src/content/docs/cli/index.md</files>
  <action>
Add a new "## Global Flags" section after the existing "### Environment Variables" section (around line 82). Include a table with these columns: Flag, Default, Env Var, Description.

Flags to document (from cli.rs research):
| Flag | Default | Env Var | Description |
|------|---------|---------|-------------|
| `--display-tz <TZ>` | `local` | `PAKYAS_DISPLAY_TZ` | Timezone for timestamps: `local` or `utc` |
| `--time <MODE>` | `both` | `PAKYAS_TIME` | Time display: `relative`, `absolute`, or `both` |
| `--no-color` | off | `NO_COLOR` | Disable colored output |
| `--plain` | off | - | Plain output without symbols/emojis |
| `--debug-http` | off | - | Print HTTP requests/responses to stderr |
| `--format <FMT>` | `table` | `PAKYAS_FORMAT` | Output format: `table`, `json`, `ndjson`, `yaml` |

Add a brief intro before the table: "These flags can be used with any pakyas command:"

Add a tip callout after the table noting that flags can also be set via environment variables for persistent configuration.
  </action>
  <verify>Read the file and confirm the Global Flags section exists with all 6 flags documented in a table format.</verify>
  <done>Global Flags section added with all 6 flags documented including their defaults, env var mappings, and descriptions.</done>
</task>

<task type="auto">
  <name>Task 2: Update Check Management section with new flags</name>
  <files>src/content/docs/cli/index.md</files>
  <action>
In the "### Check Management" section (around lines 108-151), update the examples to include:

1. Add check delete examples with -y/--yes flag (currently delete is not shown at all):
   - Add after the pause/resume examples:
   ```bash
   # Delete a check (prompts for confirmation)
   pakyas check delete my-check

   # Delete without confirmation (use in scripts)
   pakyas check delete my-check -y
   pakyas check delete my-check --yes
   ```

2. Expand the existing check logs example (line 150: `pakyas check logs backup-nightly --limit 50`):
   - Add comment explaining the default and provide additional examples:
   ```bash
   # View ping history (default: 50 entries)
   pakyas check logs backup-nightly

   # Limit to specific number of entries
   pakyas check logs backup-nightly --limit 100
   pakyas check logs backup-nightly --limit 10
   ```
  </action>
  <verify>Read the Check Management section and confirm both delete -y/--yes and logs --limit are documented with examples.</verify>
  <done>Check Management section includes delete command with -y/--yes examples and expanded logs --limit documentation.</done>
</task>

<task type="auto">
  <name>Task 3: Add See Also links for new command pages</name>
  <files>src/content/docs/cli/index.md</files>
  <action>
Update the "## Next Steps" section (around line 212) to include links to the new command pages created in Phase 1 and 2:

Add these links in logical order:
- [auth command](./auth/) - Manage authentication and stored credentials
- [api-key command](./api-key/) - Create and manage API keys
- [check doctor](./check-doctor/) - Diagnose check configuration issues
- [check inspect](./check-inspect/) - View detailed check state
- [check tail](./check-tail/) - Stream check events in real-time
- [completion command](./completion/) - Shell completion setup

Keep existing links (monitor, ping, update, External Monitors) at the end.
  </action>
  <verify>Read the Next Steps section and confirm all 6 new command pages are linked.</verify>
  <done>Next Steps section includes links to all 6 new command documentation pages.</done>
</task>

</tasks>

<verification>
1. Run `npm run build` to verify no Markdown syntax errors
2. Check that cli/index.md contains:
   - "## Global Flags" section with 6-row table
   - Delete command with -y/--yes examples
   - Logs command with --limit examples
   - Links to auth, api-key, check-doctor, check-inspect, check-tail, completion
</verification>

<success_criteria>
- [ ] Global Flags section exists with all 6 flags in table format
- [ ] Check delete command documented with -y/--yes flag
- [ ] Check logs command documented with --limit flag and examples
- [ ] Next Steps includes links to all 6 new command pages
- [ ] Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-updates-navigation/03-01-SUMMARY.md`
</output>
